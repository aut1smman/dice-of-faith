export type Velocity = {
    x: number;
    y: number;
};

export type Coordinates = {
    x: number;
    y: number;
};

export type textPositionAndRotation = {
    position: THREE.Vector3;
    rotation: THREE.Vector3;
};

